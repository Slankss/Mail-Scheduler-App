services:
  mail-scheduler:
    build: .
    image: mail-scheduler:latest
    ports:
      - "8000:5000"
    environment:
      - DATA_DIR=/app/data
      - ZERUH_API_KEY=${ZERUH_API_KEY}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
